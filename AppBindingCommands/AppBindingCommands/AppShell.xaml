<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="AppBindingCommands.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:AppBindingCommands"
    xmlns:views="clr-namespace:AppBindingCommands.Views"
    Shell.FlyoutBehavior="Disabled"
    Title="AppBindingCommands">

    <!-- Página Principal -->
    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />

    <!-- Rota para a página de usuário -->
    <ShellContent
        Title="Usuário"
        ContentTemplate="{DataTemplate views:UsuarioView}"
        Route="UsuarioView" />
</Shell>